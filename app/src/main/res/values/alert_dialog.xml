<resources>
    <style name="alert_dialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Background color -->
        <item name="android:background">@drawable/plain_background</item>

        <!-- Title text color -->
        <item name="android:textColorPrimary">@color/tata_blue</item>

        <!-- Message text color -->
        <item name="android:textColorSecondary">@color/royal_blue</item>

        <!-- Apply custom button styles -->
        <item name="android:buttonBarPositiveButtonStyle">@style/PositiveButtonStyle</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/NegativeButtonStyle</item>
    </style>

    <!-- Positive Button Style -->
    <style name="PositiveButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:background">@drawable/blue_rounded_button</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
    </style>

    <!-- Negative Button Style -->
    <style name="NegativeButtonStyle" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:background">@drawable/blue_rounded_button</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:paddingLeft">24dp</item>
        <item name="android:paddingRight">24dp</item>
    </style>
</resources>
